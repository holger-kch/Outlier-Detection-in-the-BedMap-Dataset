# Results Metadata

This folder keeps small JSON artifacts from the GNN run:

- cross-region fold sizes,
- selected Optuna configuration,
- feature statistics,
- graph and edge-attribute metadata,
- fold training summaries.

Large generated files are intentionally excluded: model checkpoints, feature
arrays, graph arrays, score parquets, and compressed seed-score caches.

