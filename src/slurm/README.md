# Slurm Wrappers

Cluster wrappers for the GNN stages:

- seed map / seed construction,
- feature, edge-attribute, and fold preparation,
- hyperparameter search,
- fold training,
- validation,
- full inference.

They assume the original NBI/HEP cluster environment and data paths.

